 * @return On error a negative value is returned, otherwise the number of bytes
 * used or zero if no frame could be decompressed.
 
 -------------------------------------------------
 在 flush 解码器时，最后没有图片时，返回 零？还是负值？
 -------------------------------------------------